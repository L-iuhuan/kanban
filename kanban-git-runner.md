# 看板流水线:Git 版本管理 + 一键运行器方案

> 需求收敛:多人测试/操作生成看板 + 代码必须版本可管理、可追溯、可回滚 + 免 Python 环境折腾。
> 核心思想:**代码不进安装包,代码进版本库;每台电脑只装"运行器",运行时从版本库拉最新代码。**

## 一、架构

```
                    你 + agent(唯一改代码的地方)
                        │  git push
                        ▼
              ┌── 内网 Git 仓库(Gitea / 共享盘裸仓库 / 公司 GitLab)
              │
   ┌──────────┼──────────┬──────────┐
   │          │          │          │
 电脑A       电脑B      电脑C      电脑D
 (测试员)    (测试员)   (操作员)   (操作员)
   │
   └─ 双击「更新并运行.bat」
        = git pull → uv sync → run_chain.py → 看板
        (日志和看板都带 commit 版本号)
```

- **代码只有一份**(仓库),不存在版本漂移
- **更新 = pull**:agent 改完 push,所有电脑一键拉最新
- **可追溯**:每次运行记录 commit hash,测试员反馈 bug 时附上版本号,你立刻知道他在跑哪个代码
- **可回滚**:改坏了?git revert / 让测试员 checkout 上一版
- **多版本并行测试**:测试员 checkout 不同分支,各测各的,互不干扰

## 二、每台电脑只需一次性装两样东西

| 组件 | 安装 | 说明 |
|---|---|---|
| Git | Git for Windows 一次性安装(或运行器里用 zip 下载替代,见下) | 版本拉取 |
| uv | 一次性安装(15MB) | Python + 依赖全自动管理,锁文件保证各电脑版本一致 |

装完后,同事日常只有**一个动作:双击「更新并运行.bat」**。

## 三、一键运行器的内容(核心交付物)

```bat
@echo off
cd /d %~dp0app

echo [1/3] 拉取最新代码...
git pull --ff-only
if errorlevel 1 ( echo [!] 更新失败,请找 XX 处理 & pause & exit /b 1 )

echo [2/3] 同步依赖(有变化才会装)...
uv sync --frozen

echo [3/3] 运行流水线...
uv run python run_chain.py

echo 完成!看板在 dashboarddashboard_a.html
pause
```

加分项(强烈建议):
- **每次运行写版本日志**:run_chain.py 开头记录 `git rev-parse --short HEAD` + 时间,写进 `outputun_meta.json`,并在看板页脚显示"v1.3.2 @ a1b2c3d · 2026-08-15 14:03"
- **测试员反馈 bug 时**:要求附上页脚版本号,你调试不用猜
- **分支约定**:main = 稳定版(操作员用),dev = 测试版(测试员 checkout dev)

## 四、Git 仓库放哪(三选一,按公司现状)

| 方式 | 成本 | 适合 |
|---|---|---|
| **共享盘裸仓库**:`git clone \\\\server\\share\\kanban.git` | 0 元,10 分钟 | 公司无任何 Git 服务,有内网共享盘 |
| **Gitea**:一台内网电脑跑个 exe,浏览器看版本历史/对比 | 半天 | 想要 web 界面、多仓库、权限 |
| 公司现成 GitLab/Gitee | 0 | 大厂直接走公司流程 |

## 五、如果同事连 Git 都不想装(可选降级)

运行器不用 git 命令,改为**从 Gitea/GitLab 下载仓库 zip**:

```
更新 = PowerShell 下载 http://内网/repo/archive/main.zip → 解压覆盖 app```

- 同事零安装(只要 uv);版本号仍可从 zip 内容或服务端 API 拿 commit
- 缺点:增量更新退化为全量下载(代码 2.3MB,zip 后几百 KB,无所谓)
- 建议:先用 git 方案,真遇到"装 git 都嫌麻烦"的同事再上 zip 版

## 六、Tauri 的位置(可选壳,版本管理不变)

如果后面想要 GUI,把运行器做成 Tauri 应用,界面是:

```
┌────────────────────────────────────┐
│  看板流水线  v1.3.2 (commit a1b2c3d)│
│  [🔄 检查更新]  [▶ 运行]  [📊 打开看板] │
│  ████████████░░░░ 进度 68%          │
│  日志:silver → gold → 渲染看板...    │
└────────────────────────────────────┘
```

- 后端逻辑不变:仍是 git pull + uv run(通过 sidecar 调)
- **版本管理还是 Git**,Tauri 只是把命令行包成了按钮
- 优先级:低。先用 bat 把流程跑顺,确认同事操作频率后再决定要不要壳

## 七、落地顺序(1~2 天)

1. 把看板流水线初始化成 git 仓库,建 main/dev 分支(半小时)
2. 部署仓库:共享盘裸仓库或 Gitea(半天,含写使用说明)
3. 每台电脑装 Git + uv,放「更新并运行.bat」(1 小时/台,或写个安装脚本)
4. run_chain.py 加 commit 版本记录 + 看板页脚显示(1 小时)
5. 找 2~3 个同事试跑一周,收集反馈

## 八、为什么这个方案解决了你的全部痛点

| 痛点 | 解决 |
|---|---|
| 版本管理 | Git 中央仓库,单点改代码,全程可追溯 |
| 多人测试 | 每人一键拉最新/切换分支,版本号写进看板,反馈不再对不上 |
| 环境问题 | uv 锁文件 + 一次性安装;依赖变动 uv sync 自动同步 |
| 更新分发 | push 后同事一键 pull,零打包、零重发 |
| agent 迭代 | agent 改完 → 你 review → push → 全员生效,循环极短 |

一句话:**你的需求本质是"代码分发 + 版本治理",这是 Git 的主场,不是打包器的主场。运行器负责"跑",Git 负责"管",uv 负责"环境",三者各司其职。**
