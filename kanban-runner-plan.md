# 看板流水线运行器(Tauri)· 开发规划 v0.1

> 目标:测试人员(纯小白)双击一个应用,自动同步最新代码 → 选 Excel → 跑流水线 → 看进度日志 → 打开看板和中间产物。
> 版本管理在共享盘(git),代码自动同步,小白全程无感。

## 1. 架构总览

```
共享盘 \\<server>\<share>\kanban-repo        (开发者维护,git 仓库)
├─ processing/  dashboard/  run_chain.py ...    ← 代码(同步源)
├─ version.txt                                  ← 开发者 push 时自动生成(commit + 时间)
└─ (data/output 不同步,保护本地成果)

每台测试电脑 D:\KanbanApp\
├─ KanbanRunner.exe          ← Tauri 应用(小白双击)
├─ code\                     ← 代码缓存(应用启动时自动从共享盘同步)
├─ python\                   ← Python 3.11 运行时(装机时装一次)
├─ venv\                     ← 依赖环境(setup 时建好,依赖变化自动增量安装)
└─ config.json               ← 共享盘路径等配置(首次运行时填写)

                应用内部(Rust 壳)
   ┌──────────────────────────────────────┐
   │ ① 启动:检查配置 → robocopy 同步代码    │
   │ ② 界面:选 Excel(拖拽/按钮) → 点生成    │
   │ ③ 运行:spawn python run_chain.py      │
   │     stdout 流式转发前端日志面板         │
   │ ④ 完成:按钮亮起                        │
   │     [打开看板] [打开中间产物] [打开日志] │
   └──────────────────────────────────────┘
```

## 2. 界面草图(暗色,风格沿用 tauri-demo)

```
┌──────────────────────────────────────────────────────┐
│ 🦀 看板流水线          v1.3.2 @ a1b2c3d    ● 已连接  │
├──────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐    │
│  │   📥 把 Excel 拖到这里,或 [选择文件]           │    │
│  │   当前: 财务分析-8月.xlsx                      │    │
│  └──────────────────────────────────────────────┘    │
│  [ ] 跳过数据处理(只重新生成看板)                     │
│                                                      │
│  [ ▶ 生成看板 ]    [ ■ 停止 ]                         │
│                                                      │
│  ████████████████░░░░░░░░  阶段: 产品分析(4/6)       │
│                                                      │
│  ┌── 运行日志 ──────────────────────────────────┐    │
│  │ 14:03:01 [OK] 代码已同步(3 个文件更新)        │    │
│  │ 14:03:02 [--] 开始处理: silver 清洗...        │    │
│  │ 14:03:45 [--] gold 汇总完成                   │    │
│  │ 14:04:12 [!!] 警告: 客户"XX科技"名称模糊匹配  │    │
│  │ ...                                            │    │
│  └────────────────────────────────────────────────┘    │
│                                                      │
│  [📊 打开看板]  [📁 中间产物]  [📁 输出目录]  [⚙ 设置] │
└──────────────────────────────────────────────────────┘
```

## 3. 关键设计决策

| 决策点 | 方案 | 理由 |
|---|---|---|
| 同步机制 | robocopy /MIR,排除 .git/output/data | Windows 自带、无需装 git、增量快;排除项保护本地成果 |
| 版本标识 | version.txt(开发者 push 钩子生成)→ 界面顶栏 + 看板页脚 | 小白不碰 git,版本号自动注入 |
| Python 环境 | 本地 D:\KanbanApp\python + venv;setup 时装一次,依赖变化应用内自动 pip install | 环境绝不放共享盘(加载慢/锁文件) |
| 运行方式 | Rust spawn python,stdout 逐行 emit 事件到前端 | 真·实时日志;退出码判定成功失败 |
| 进度条 | 解析日志中的阶段标记(改造 run_chain.py 输出 [STAGE x/6] 行,向后兼容) | 精确进度,而非假动画 |
| 停止按钮 | 杀进程树(Windows job object 或 taskkill /T) | 长任务可中断 |
| 配置 | config.json:共享盘路径、Python 路径;首次运行弹设置向导 | 换服务器不用重装 |

## 4. Rust commands(前端调用)

| command | 参数 | 返回 |
|---|---|---|
| `sync_code` | — | {updated, changed_files, version} |
| `run_pipeline` | data_path, skip_processing | job_id(后台 spawn) |
| `stop_pipeline` | job_id | 停止结果 |
| `open_dashboard` | — | 用默认浏览器打开看板 HTML |
| `open_folder` | kind: output/gold/report | explorer 打开对应目录 |
| `get_status` | — | {share_ok, python_ok, version, last_run} |
| `save_config` / `get_config` | path 等 | 配置读写 |

日志与进度:spawn 后 Rust 侧读 stdout 线程 → `emit("pipeline-log", line)` + `emit("pipeline-stage", {n,total,name})` → 前端监听渲染。结束 `emit("pipeline-done", {code, duration})`。

## 5. 环境安装(每台电脑一次性,setup.bat)

```
setup.bat(随应用文件夹分发):
1. 静默安装 Python 3.11(若没有,自动下载官方安装包或从共享盘取)
2. python -m venv D:\KanbanApp\venv
3. venv\pip install -r requirements.txt(首次 5~10 分钟,依赖装好后可拷给下一台复用)
4. 创建桌面快捷方式「看板流水线」
之后日常:小白双击快捷方式,一切自动。
```

依赖变化时:应用启动时比对 requirements.txt 的 hash,变了就后台 `pip install`(界面提示"正在更新组件…")。

## 6. 开发者工作流(你 + agent)

```
agent 改代码 → 你 review → git push 到共享盘仓库
  → push 钩子自动写 version.txt(commit + 时间)
测试员下次双击应用 → 自动同步 → 界面显示新版本号 → 开跑
测试员报 bug → 附上看板页脚版本号 → 你精确对应代码版本
```

## 7. 开发里程碑

| # | 任务 | 产出 | 预估 |
|---|---|---|---|
| M1 | Tauri 壳 + 界面骨架(日志面板/进度条/按钮) | 可运行的壳 | 0.5 天 |
| M2 | 同步命令(robocopy)+ 版本显示 + 配置向导 | 自动更新代码 | 0.5 天 |
| M3 | 运行命令(spawn + stdout 流 + 停止)+ 阶段解析 | 能跑流水线 | 1 天 |
| M4 | 打开看板/产物 + 运行状态机(空闲/同步/运行/完成/失败) | 完整闭环 | 0.5 天 |
| M5 | setup.bat + requirements hash 增量安装 | 装机自动化 | 0.5 天 |
| M6 | 联调真实流水线 + 异常路径(断网/共享盘不可达/Excel 占用) | 可交付 | 1 天 |
| M7 | 看板页脚注入版本号(改 generate_dashboard.py,一行) | 版本追溯 | 0.5 天 |

## 8. 需要你拍板的开放问题

1. **共享盘路径**:测试电脑访问代码的 UNC 路径是什么?(例 `\\192.168.x.x\share\kanban-repo`)——不确定就先做配置向导,首启填写。
2. **Python 3.11 还是你现有版本?**(requirements 兼容性)
3. **看板产物文件名**:固定 `dashboard\dashboard_a.html` 还是会变?(M4/M7 依赖)
4. **中间产物目录**:output\silver、output\gold、output\report 三个都要入口吗?
5. **同步时机**:启动时自动同步一次 + 手动"检查更新"按钮,够吗?还是运行前再同步一次?
6. **多版本共存**:测试员需要能选择跑"稳定版/测试版"吗?(v1 先不做,统一跑 main)

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 共享盘不可达/断网 | 界面红色状态灯 + 提示;代码缓存仍在,可离线跑旧版 |
| Excel 被占用 | 跑前检查文件锁,提示关闭 Excel(README 已有此坑) |
| 流水线跑 10 分钟假死 | 日志实时滚动 + 阶段进度,告别"黑窗口不动"的焦虑 |
| 多人同时同步/运行 | 同步只读共享盘,无冲突;各自本地运行互不影响 |
| 杀软拦 python spawn | 装机时加白名单提示(可选) |
```
