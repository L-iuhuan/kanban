# 看板流水线:方案全景补充(基于真实场景修正)

> 场景修正后的三个角色:
> - **受众**(看板发出去给他们看)→ 只收 HTML,天然零环境
> - **生成方**(不同电脑上跑数据出看板)→ 环境痛点在这里
> - **开发者**(你 + agent,方案更新/bug 修复高频迭代)→ 更新分发痛点在这里
>
> 代码规模:母项目 semiconductor_analysis 242 个 py / 2.3MB,优化版看板流水线 84 个 py / 788KB

## 关键认知修正

1. **受众不需要应用** —— 看板是 HTML,双击就开。你只需要给生成方做应用。
2. **你的迭代频率是方案级的**(母项目里 fix_*.py / res_*.md / deep_*.md 一堆,迭代极密)——"更新分发成本"和"环境成本"同等重要,之前方案没重点考虑。
3. 母项目已有 8 个 bat 菜单 + 检测 Python + 红字报错 —— 小白体验的骨架已经有了,缺的是"免装 Python"。

## 方案全景(7 个,按"你最该看"排序)

### ① 便携 Python 文件夹 —— 更新 = 覆盖几个 .py ⭐ 强烈推荐

- 把 Python 解释器 + 全部依赖装进一个文件夹(WinPython 或 embeddable Python + pip),拷到哪台电脑都能跑,不写注册表、不装系统
- **杀手锏:代码明文。agent 改完 bug → 只发改动的几个 .py(几 KB)→ 覆盖目标机文件 → 完成更新**。不用重新打包 250MB
- 配合已有 bat 菜单:把 bat 里的 python 换成便携版路径,小白双击体验不变
- 工作量:**1~2 天**(打包依赖 + 改 bat)
- 缺点:代码明文可见(内部工具无所谓);首次做依赖缓存要细心

### ② 内网 Web 服务(FastAPI)—— 如果有一台常开机器,这是一劳永逸解 ⭐

- 数据在"不同电脑"、看板"发出去看"、代码"频繁改"—— 教科书级内网 Web 场景
- 一台常开电脑(或 NAS/小主机)跑 FastAPI:
  - 其他人浏览器上传 Excel → 服务端排队处理(8~10 分钟任务转后台异步)→ 在线看板 + 下载 HTML
  - **代码更新只在服务器一处改,全员立即生效,客户端永远只有浏览器**
- 彻底消灭"多机环境"+"更新分发"两个问题(不是缓解,是消灭)
- agent 友好度最高:FastAPI 是 agent 强项,部署就是 pip install uvicorn
- 工作量:2~4 天(agent 加速后);缺点:需要一台常开的机器

### ③ 单文件 HTML 看板 —— 受众端分发增强(顺手做)

- template.html(289KB)+ 数据 → 内联成**一个 dashboard.html**(数据+JS 全在文件里)
- 微信/邮件都能直接发,收件人双击即看,不存在"发给别人还要带文件夹"
- 看板 30MB 是因为数据大,可做成"看板壳 + 数据文件"或压缩内联;如果数据几 MB 以内直接单文件
- 工作量:0.5~1 天(改 generate_dashboard.py 的输出方式)

### ④ PyInstaller 打包(之前的方案 B)—— 保留但降级

- 仍是最省心的"免环境"方案,但有硬伤:**每次改代码要重新打包 10~30 分钟 + 重发 250MB**,与你的高频迭代直接冲突
- PyInstaller 调试是 agent 易卡区(隐式依赖、二进制钩子、杀软误报)
- 适用:迭代稳定后出"正式版"给完全不懂电脑的人

### ⑤ Tauri 壳 + sidecar(之前的方案 C)—— 价值进一步收窄

- 若生成方同事必须是"纯图形界面"(拖文件、看进度条、点按钮),Tauri 壳体验最好,且看板模板 100% 复用
- 但注意:你的生成方现在用 bat 菜单已经能跑(说明能接受"双击+等待"),Tauri 壳是锦上添花不是雪中送炭
- 若做,sidecar 打包复用 ④ 的成果;Tauri updater(内网静态服务器托管)可顺带解决更新分发
- 工作量:1~2 周;agent 易卡点:sidecar 进程管理、权限模型、打包工具链

### ⑥ uv 工具链 —— 现代 pip 的"半自动环境"

- 生成方装一次 uv(15MB)→ `uv run run_chain.py` 自动装 Python + 依赖(锁文件保证版本一致)
- 比"装 Python + pip install"体验好一个量级;但对纯小白仍有"装 uv"一步
- 适合:生成方是"半技术"同事,且你不想维护便携文件夹

### ⑦ 全 Rust 重写(之前的方案 A)—— 即使有 agent 也不推荐

- 2.3MB 业务代码 + statsmodels/sklearn 在 Rust 无成熟等价物
- **agent 写代码快,但瓶颈是"统计结果对拍验证"**:新旧实现逐指标 diff、判断 0.001 差异是否可接受,需要领域判断,agent 只能辅助
- 唯一收益(30MB 体积)对这个内网分发场景意义不大

## 关于 agent 的现实判断(你问的"难度主要看 agent 能力")

| 环节 | agent 表现 | 备注 |
|---|---|---|
| 写 FastAPI / 改 Python / 写 Rust | ✅ 强项 | ②⑤⑦ 的编码部分会很快 |
| 写对拍测试(新旧实现 diff) | ✅ 能做 | 但"diff 到什么程度算通过"要你拍板 |
| 便携 Python 依赖收集 | ✅ 能做 | pip freeze + 复制 site-packages,套路固定 |
| PyInstaller spec / 隐式依赖 | ⚠️ 常卡 | pandas/sklearn 的 data 文件、二进制钩子,要反复试 |
| Tauri sidecar / 权限模型 | ⚠️ 常卡 | 我们这次 demo 就踩了 4~5 个沙箱/权限坑 |
| Windows 打包工具链(NSIS/WiX/签名) | ⚠️ 常卡 | 下载墙、杀软误报,环境问题非代码问题 |
| 统计正确性验证 | ❌ 依赖你 | agent 能跑数字,不能替你判断业务对错 |

**结论:agent 时代,编码成本不再主导,主导的是"打包/分发/验证"这些环境与正确性环节。** 所以方案排序向"打包环节最简单"倾斜:①②(几乎零打包)> ④(打包是 agent 弱项)> ⑤(打包+sidecar)> ⑦(验证地狱)。

## 推荐组合(按你的场景)

**短期(本周,零风险)**:
- 做 ① 便携 Python 文件夹(1~2 天),生成方拷文件夹即用,更新只发 .py
- 顺手做 ③ 单文件 HTML 看板(0.5~1 天),受众端分发体验拉满

**中期(观察后二选一)**:
- 若你公司有一台常开内网机器 → 上 ② FastAPI 服务,一劳永逸
- 若必须纯离线单机 → 上 ④ PyInstaller 正式版(迭代冻结期)+ 可选 ⑤ Tauri 壳

**不做**:⑦ 全 Rust 重写。

一句话:**环境问题的答案是"把环境带走"(①/④)或"把环境集中"(②);Tauri 只在你需要"小白 GUI"时作为壳出现;agent 让编码变便宜,但打包和验证的坑它替不了你踩。**
