# 看板助手 · 更新策略

> 版本:v0.3.4 起 | 最后核验:2026-08-17 | 代码位置:`kanban-runner/src-tauri/src/lib.rs`(check_app_update / self_update / version_newer)

## 一、两条更新通道

| 通道 | 共享盘位置 | 内容 | 客户端动作 |
|---|---|---|---|
| **流水线代码** | `<share>\code\` | run_chain.py + version.txt + deps.txt | 「更新代码」按钮 / 启动时同步 / 2 分钟巡检高亮 |
| **壳子(应用本体)** | `<share>\app\` | `*-setup.exe` + `app-version.txt` | 启动时检查 -> 横幅提示 -> 一键自动更新 |

两条通道互相独立:改流水线代码只需发布 `code\`;只有壳子本身(Rust/前端)变了才需要发布 `app\`。

## 二、壳子自更新完整流程

### 1. 检查(读)
- 时机:应用启动时 + 设置页「检查更新」按钮
- 前提:共享盘可达(不可达时跳过检查,避免 UNC/SMB 超时卡启动,可达数十秒)
- 逻辑:读 `<share>\app\app-version.txt`,与编译期版本 `CARGO_PKG_VERSION` 比较
- 结果:远端更新 -> 前端弹蓝色横幅「发现新版本 vX.Y.Z,可一键自动更新」

### 2. 版本比较语义(version_newer)
- 按 `.` 分段数值比较,允许 `v` 前缀;段缺失按 0 处理;解析失败的段按 0 处理
- 示例:`0.3.4 > 0.3.3` ✅ | `v0.4.0 > 0.3.3` ✅ | `0.3.3 = 0.3.3`(不提示)✅ | `0.3.2 < 0.3.3`(不提示)✅ | `0.3.10 > 0.3.9` ✅
- 已用 7 组用例单测验证通过(2026-08-17)

### 3. 一键更新(写,self_update 命令,v0.3.5 重构)
1. 复核版本(区分「已最新」与「未找到」,与启动检查的 None 语义不同)
2. 扫描 `<share>\app\` 下 **以 `-setup.exe` 结尾**的 exe,按**修改时间**取最新
3. 拷贝到 `%TEMP%\KanbanPipeline-update.exe`(覆盖旧文件)
4. 生成 `%TEMP%\KanbanPipeline-update.cmd` 批处理(CRLF 行尾)并隐藏执行:
   - **等待旧进程真正退出**:`tasklist` 轮询 `kanban-runner.exe`(每秒一次,最多 60 次,超时 `taskkill /F` 兜底)——v0.3.4 及以前用固定 `ping -n 3` 等 2 秒,旧进程没退干净时 NSIS 静默安装会中止且无任何提示,此为实测踩过的坑
   - 执行安装包 `/S` 静默安装(装到原目录 `%LOCALAPPDATA%\看板助手\`,NSIS currentUser 模式固定,不会换目录)
   - **写结果文件**:安装器退出码写入 `%LOCALAPPDATA%\KanbanRunner\update-result.txt`
   - `start` 重开当前 exe 路径 → 批处理自删除
5. 500ms 后本进程退出(IPC 响应先送达前端)

### 3a. 更新结果回执(v0.3.5 新增)
- 前端在更新遮罩期间退出;重启后 init 调用 `take_update_result` 命令,读取并删除 `update-result.txt`
- `"ok"` → 轻横幅「已成功更新到当前版本」(数秒自动消失);`"fail:N"` → 错误横幅「上次自动更新失败(安装器退出码 N),已回退旧版,可重试」
- 前端体验:设置弹层内可直接点「立即更新」(无需关弹层找横幅);更新全程有全屏三步遮罩(关闭应用 → 静默安装约 10-30 秒 → 自动重启)

### 4. 发布(publish_to_share.ps1)
```powershell
# 只发代码
.\tools\publish_to_share.ps1
# 同时发壳子安装包(版本号写进 app\app-version.txt)
.\tools\publish_to_share.ps1 -AppInstaller <setup.exe路径> -AppVersion 0.3.4
```

## 三、硬性约束(违反即更新链断裂)

1. **`productName` 永远不要再改**(现为「看板助手」)。改名 = 安装目录变(`%LOCALAPPDATA%\看板助手\`),旧版卸不干净、双图标;更严重:老客户端点更新后,新版本装到新目录,但 `self_update` 用 `current_exe` 重开的是**旧 exe**,表现为「更新完还是旧版」且每次启动再提示、死循环。v0.3.1(KanbanPipeline)-> v0.3.3(看板助手)已踩过此坑,因尚未分发未造成事故。
2. **安装包文件名必须以 `-setup.exe` 结尾**(客户端按此后缀过滤)。
3. **`app-version.txt` 内容为纯语义化版本号**(如 `0.3.4`),UTF-8 无 BOM,不要带日期/哈希等附加信息。
4. 安装模式为 NSIS **currentUser**(装到 `%LOCALAPPDATA%\看板助手\`,免管理员);静默安装用 NSIS 标准 `/S` 参数。
5. 重开路径用 `current_exe` 而非硬编码——历史上硬编码 `%LOCALAPPDATA%\KanbanPipeline` 曾导致装完静默失败(v0.3.3 开发期已修复)。

## 四、验证状态(2026-08-18)

| 环节 | 方式 | 结果 |
|---|---|---|
| 发布脚本写通道(文件名/版本文件格式) | 本地模拟共享盘实测 | ✅ 与客户端读取逻辑吻合 |
| 版本比较 version_newer | 7 用例单测 | ✅ 全过 |
| NSIS 静默安装 `/S`(装原目录) | 真机实测(0.3.3~0.3.7 包) | ✅ 退出码 0、目录/注册表/版本正确 |
| GUI 完整闭环 0.3.3→0.3.4(旧更新器) | 用户实测 | ❌ ping 固定等 2s 不够,旧进程未退净,安装中止且无任何提示 → v0.3.5 重构 |
| GUI 完整闭环 0.3.5→0.3.6(重构版) | 用户实测 | ❌ 安装成功,但 .cmd 为 UTF-8 被 GBK cmd 误读,重开路径乱码(`鐪嬫澘鍔╂墜`)无法自动重开 → v0.3.7 修复 |
| GUI 完整闭环 0.3.7→0.3.8(ASCII 模板+env 传参) | 用户实测 | ✅ **全链路通过**:设置内一键更新 → 全屏三步遮罩(≥2s) → 静默安装 → 中文路径自动重开 → 启动时消费结果回执 |

## 五、本地 E2E 测试指引

1. 本机安装当前正式版(如 0.3.3):双击 `installer\看板助手_0.3.3_x64-setup.exe`
2. 准备模拟共享盘(仅 `app\` 通道即可触发更新提示):
   ```
   <任意目录>\app\app-version.txt        <- 内容:0.3.4(比已装版本新)
   <任意目录>\app\看板助手_0.3.4_x64-setup.exe
   ```
3. 启动应用 -> 设置里把「共享盘路径」指向该模拟目录
4. 点「检查更新」-> 应弹蓝色横幅 -> 点「立即更新」
5. 预期:应用自动关闭 -> 静默安装(约 10-30s,无窗口) -> 自动重开
6. 核验:设置页「检查更新」提示已是最新;安装目录 exe 版本 = 0.3.4;注册表 DisplayVersion = 0.3.4

## 六、运维提示

- 共享盘真实路径:`\\192.168.8.3\财务部\办公软件\SoftwareUpdate\数据分析看板`(客户端内置为默认值,可在设置改)
- 客户端断网/共享盘不可达时:更新检查自动跳过,不影响本地缓存跑流水线(离线模式)
- 若某次更新失败(如安装包损坏),横幅会持续提示,重新点「立即更新」即可重试;`%TEMP%\KanbanPipeline-update.exe` 会被下次更新覆盖
